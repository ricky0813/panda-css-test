import { CssKeyframes } from "@styled-system/types";

const keyframes: CssKeyframes = {
  buttonSpin: {
    from: {
      transform: "rotate(0turn)",
    },

    to: {
      transform: "rotate(1turn)",
    },
  },
};

export default keyframes;
